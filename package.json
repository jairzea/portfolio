{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && touch dist/.nojekyll && git add -f dist && git commit -q -m \"chore: build\" && git subtree split --prefix dist -b gh-pages-tmp && git push -f origin gh-pages-tmp:gh-pages && git branch -D gh-pages-tmp && git reset -q --soft HEAD~1 && git restore --staged dist"
  },
  "dependencies": {
    "motion": "12.23.12",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "4.1.11",
    "@types/react": "19.1.8",
    "@types/react-dom": "19.1.6",
    "@vitejs/plugin-react": "5.0.0",
    "tailwindcss": "4.1.11",
    "typescript": "5.8.3",
    "vite": "7.3.6"
  }
}
